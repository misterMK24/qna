$('.answer-errors').html('<%= render 'shared/errors', resource: @answer %>')

<% if @answer.persisted? %>
  $('.answers').append('<%= j render @answer %>')
  $("textarea[id='answer_body']:visible").val('')
  $('.answers_count').text('<%= j pluralize(question.answers.count, 'Answer') %>')
<% end %>
